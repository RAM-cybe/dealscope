# secrets
.env
*.env
.env.*
*_api_key*
*apikey*

# python
__pycache__/
*.pyc

# os
.DS_Store

# claude code local settings + launch config (machine-specific paths)
.claude/

# streamlit / local runtime logs
*.log
streamlit.log

# source PDFs and OCR/extraction scratch (kept local only)
*.pdf
*.txt
!requirements.txt
sources/
_tmp_ocr/
extract_tmp/

# superseded / partial data files (kept local only)
companies_full.csv
companies_partial_425.csv
companies_failed.csv
deals.csv
deals_full.csv
deals_partial_*.csv
sector_notes_partial_*.csv
_v2_pledge_cache.csv
_v2_roce_cache.csv
_v2_net_income_cache.csv
_v2_currency_cache.csv
remaining_symbols.txt
pull_progress.log
pull_net_income_progress.log
fix_currency_progress.log
EQUITY_L.csv

# one-time data-collection scripts (kept local only)
# enrich_v2.py removed from this list 2026-07-20: no longer a one-time script --
# archive/data_pipeline_scripts/backfill_core_fields_2026-07.py now imports
# fetch_roce()/fetch_pledge() from it as an ongoing dependency, so it needs to
# be in version control for that script to be runnable by anyone but the
# machine that happened to have it on local disk already.
merge_build.py
pull_remaining.py
reclassify_deals_v2.py
pull_net_income.py
fix_net_income_currency.py

# redundant / unrelated / superseded local-only material (root copies only --
# the historical copy under archive/old_planning_docs/ is intentionally kept)
files.zip
knowledge-base:/
/RESEARCH_SUMMARY.md
/sector_context.md

# design reference mockups (not part of the shipped single-file app)
MA Screener - Design Options.dc.html
DealScope - Final Design.dc.html

# leftover local scratch (test CSV export, stray checklist)
nse_ma_screener_results.csv
SHIP_LIKE_A_PRO_CHECKLIST.md

# runtime-generated AI rationale cache (regenerates on first request per company)
.rationale_cache.json
